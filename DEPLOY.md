# 배포 (GitHub → Render)

폰으로 QR 을 찍어 들어오려면 인터넷에서 접근 가능한 주소가 필요하다.
아래 순서대로 하면 된다. **아이디·비밀번호가 필요한 단계는 직접 해야 한다.**

로컬 git 저장소와 첫 커밋은 이미 만들어져 있다. `git log` 로 확인할 수 있다.

---

## 1. GitHub 저장소 만들기

https://github.com/new 에서 저장소를 하나 만든다.

- 이름: 아무거나 (예: `mafia-game`)
- 공개/비공개: **둘 다 된다.** Render 는 비공개 저장소도 연결할 수 있다.
  소스를 남에게 보이고 싶지 않으면 Private 을 고르면 된다.
- **README, .gitignore, license 는 체크하지 말 것** (이미 있어서 충돌난다)

만들고 나면 나오는 주소를 아래에 넣고 실행한다.

```bash
git remote add origin https://github.com/<사용자명>/<저장소명>.git
git branch -M main
git push -u origin main
```

## 2. Render 에 올리기

1. https://render.com 에 GitHub 계정으로 로그인
2. **New → Blueprint** 선택
3. 방금 만든 저장소를 고른다 (`render.yaml` 을 자동으로 읽는다)
4. `PUBLIC_URL` 환경변수를 물어보면 **일단 아무 값이나** 넣고 배포한다
5. 배포가 끝나면 `https://xxxx.onrender.com` 주소가 나온다
6. **Environment → `PUBLIC_URL` 을 그 주소로 고치고 저장** → 자동 재배포

> ⚠️ **6번을 빼먹으면 QR 이 localhost 를 가리켜서 다른 폰이 못 들어온다.**
> 이게 배포에서 제일 자주 나는 실수다.

## 3. 확인

1. 폰 브라우저로 `https://xxxx.onrender.com` 접속
2. 방 만들기 → QR 이 뜬다
3. 다른 폰 카메라로 그 QR 을 찍어 입장되면 성공

---

## 무료 플랜에서 알아둘 것

- **15분 놀면 서버가 잠든다.** 다시 깨우는 데 30초~1분 걸린다.
  모임 시작 5분 전에 한 번 접속해서 깨워두면 된다.
- **서버가 잠들거나 재시작되면 진행 중이던 방이 사라진다.**
  게임 상태는 전부 메모리에만 있기 때문이다. 한 판 도중에는 잠들지 않으니
  실제 플레이에는 문제없지만, 중간에 다 같이 30분 쉬면 방이 없어질 수 있다.
- 나레이션 음성 파일(mp3)을 넣으면 저장소 용량이 늘어난다. 무료 플랜에서도
  수십 MB 정도는 문제없다.

## 업데이트하는 법

코드를 고친 뒤:

```bash
git add -A
git commit -m "설명"
git push
```

Render 가 자동으로 다시 배포한다.

---

## 직접 서버를 돌리는 경우 (같은 와이파이)

Render 없이 집 PC 에서 돌려도 된다. 같은 공유기에 붙어 있어야 한다.

1. PC 의 내부 IP 를 확인한다 (`ipconfig` → IPv4 주소, 예: `192.168.0.5`)
2. 그 주소를 `PUBLIC_URL` 로 지정해 서버를 띄운다

```bash
$env:PUBLIC_URL='http://192.168.0.5:3000'; npm start
```

3. 윈도우 방화벽이 3000 포트를 막으면 처음 실행할 때 뜨는 창에서 허용한다

이 방식은 인터넷이 없어도 되고 서버가 잠들지도 않지만,
모두가 같은 와이파이에 있어야 한다.
